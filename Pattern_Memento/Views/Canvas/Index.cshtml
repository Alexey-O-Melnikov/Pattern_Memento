@model IEnumerable<Pattern_Memento.Domain.Canvas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CanvasID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CanvasID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CanvasID })
        </td>
    </tr>
}

</table>
